# Firebase App Hosting configuration for Next.js
runtime: nodejs20

fetch:
  - name: install
    run:
      - npm
      - ci

build:
  - name: build
    run:
      - npm
      - run
      - build

run:
  command: npm start

env:
  - variable: NODE_ENV
    value: production
